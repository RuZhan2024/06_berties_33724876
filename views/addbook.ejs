<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title><%= shopData.shopName %> — Add Book</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <link rel="stylesheet" href="/main.css" />
  </head>
  <body>
    <%# Admin-style page: form posts to books controller to create a new book. %>
    <header class="container">
      <h1>Bertie’s Books: Add Book</h1>
      <p><a href="/">← Back home</a></p>
    </header>

    <main class="container">
      <%# NOTE: using a relative action. From /books/addbook, 'bookadded' -> POST /books/bookadded %>
      <form action="bookadded" method="post" class="form card">
        <label>Book name
          <%# Required text field bound to req.body.name %>
          <input id="book_name" name="name" type="text" required />
        </label>
        <label>Price (£)
          <%# Number input with min/step validation. Server should still validate. %>
          <input id="price" name="price" type="number" min="0" step="0.01" required />
        </label>
        <%# Submits to server; expect redirect/flash on success. %>
        <button type="submit" class="btn">Add Book</button>
      </form>
    </main>
  </body>
</html>
